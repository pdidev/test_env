FROM pdidevel/centos_cmake3.5

COPY --from=pdidevel/centos_libs_cmake3.10 /usr/local /usr/local

RUN yum install -y --setopt=tsflags=nodocs \
    libparaconf-devel \
    libyaml-devel \
    spdlog-devel \
 && yum clean all -y \
 && echo '. /usr/local/bin/flowvr-suite-config.sh' >> /etc/profile.d/sh.local
